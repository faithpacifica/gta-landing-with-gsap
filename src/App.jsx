import gsap from 'gsap';
import { ScrollTrigger } from 'gsap/all';

const App = () => {
	return (
		<main>
			<div>
				<h1>Welcome to GSAP and GTA Animation with React!</h1>
			</div>
		</main>
	);
};

export default App;
